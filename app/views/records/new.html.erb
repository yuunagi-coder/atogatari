<div class="flex flex-col md:justify-center justify-start items-center h-screen w-full overflow-auto">
  <div class="w-full max-w-md mx-auto m-10">
  <h1 class="text-center text-3xl"><%= t('header.record_new') %></h1>
  <%= render "form", record: @record %>
  </div>
</div>

 
