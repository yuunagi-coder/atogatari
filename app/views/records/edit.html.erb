<div class="flex flex-col md:justify-center justify-start items-center h-5/6  md:h-screen w-full overflow-auto">
  <div class="w-full max-w-md mx-auto mt-10">
  <h1 class="text-center text-3xl"><%= t('header.record_edit') %></h1>
  <%= render "form", record: @record %>
  </div>
</div>
