<div class="flex flex-col justify-center items-center h-screen w-full">
  <h1 class="text-3xl"><%= t('header.record_index') %></h1>
  <%= render 'shared/flash_message' %>
  <div id="records" class="mx-auto w-4/5 max-w md">
    <div class="m-4">
      <%= render 'shared/search', q: @q, url: records_path %>
    </div>  
    <% if @records.any? %>
      <% @records.each do |record| %>
        <%= link_to record, class: "block" do %>
          <%= render partial: 'records', locals: { record: record } %>
        <% end %>
      <% end %>
    <% else %>
      <div class="flex flex-col justify-center mx-auto items-center">
        <p class="text-xl text-center m-10">思い出が記録されていないようです</p>
        <%= link_to  t('header.record_new') , new_record_path, class:"btn bg-yellow-500 hover:bg-yellow-600 font-bold py-2 px-4 rounded w-1/5 text-center" %>
      </div>
    <% end %>
    <%= link_to t('records.back'), 'javascript:history.back()', class: "py-2 px-4 items-left" %>
  </div>
</div>
